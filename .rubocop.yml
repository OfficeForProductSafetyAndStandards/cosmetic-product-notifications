inherit_gem:
  govuk-lint: configs/rubocop/all.yml

AllCops:
  Include:
    - 'app/**/*.rb'
    - 'db/**/*.rb'
    - 'lib/**/*.rb'
    - 'lib/**/*.rake'
    - 'test/**/*.rb'
  
  Exclude:
    - 'bin/*'
    - db/schema.rb

Rails:
  Enabled: true