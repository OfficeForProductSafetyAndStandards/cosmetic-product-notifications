default: &default
  adapter: postgresql
  encoding: unicode
  host: db
  username: postgres
  password:
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: cosmetics_development
  url: <%= ENV.fetch('DATABASE_URL', 'blah') %>

test:
  <<: *default
  database: cosmetics_test
  host: localhost
  url: <%= ENV.fetch('DATABASE_URL', 'blah') %>

production:
  <<: *default
  database: cosmetics_prod
  url: <%= ENV.fetch('DATABASE_URL', 'blah') %>
  connect_timeout: 2
  variables:
    statement_timeout: <%= ENV["STATEMENT_TIMEOUT"] || "15000ms" %>
