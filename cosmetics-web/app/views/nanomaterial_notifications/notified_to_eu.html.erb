<% content_for :page_title, "Nanomaterials" %>
<% content_for :after_header do %>
  <%= render "layouts/navbar" %>
<% end %>


<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    <h1 class="govuk-heading-xl">Nanomaterial name</h1>

    <%= form_for @nanomaterial_notification, url: notified_to_eu_responsible_person_nanomaterial_path(@responsible_person, @nanomaterial_notification) do |form| %>

      <%= render "form_components/govuk_error_summary", form: form %>


      <% date_notified_to_eu_html = render("components/govuk_date_input",
        id: "notified_to_eu_on",
        namePrefix: "notified_to_eu_on",
        fieldset: { legend: { text: "Date EU notified on" } },
        hint: { text: "For example, 31 3 2019" },
        items: [{ name: "day", classes: "govuk-input--width-2" },
                { name: "month", classes: "govuk-input--width-2" },
                { name: "year", classes: "govuk-input--width-4" }]) %>


      <%= render "components/govuk_radios",
          "idPrefix": "eu_notified",
          "name": "eu_notified",
          "fieldset": {
            "legend": {
              "text": "Was ‘#{h(@nanomaterial_notification.iupac_name)}’ notified in the EU?"
            }
          },
          "items": [
            {
              "value": "yes",
              "text": "Yes",
              "conditional": {"html": date_notified_to_eu_html}
            },
            {
              "value": "no",
              "text": "No"
            }
          ]
        %>

      <button type="submit" class="govuk-button">Continue</button>
    <% end %>

  </div>
</div>
