<% title = "EU notification ZIP files" %>
<% content_for :page_title, title %>
<% content_for :after_header do %>
  <%= link_to "Back", wizard_path(:have_products_been_notified_in_eu), class: "govuk-back-link" %>
<% end %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-xl"><%= title %></h1>
    <p>To notify your products you can upload the EU notification ZIP file or give the information manually. Uploading the EU notification ZIP file means you need to answer fewer questions – you can upload up to 100 ZIP files at a time.</p>

    <%= form_with url: wizard_path, method: :put do |form| %>
      <%= render "components/govuk_radios", name: :answer,
        errorMessage: @error_text.present? && { text: @error_text },
        fieldset: { legend: { \
            text: "Do you have the ZIP files from your EU notification?",
            classes: "govuk-fieldset__legend--m" \
          } }, items: [{ text: "Yes", value: "yes" },
          { text: "No, I’ll enter information manually", value: "no" }] %>
      <%= govukButton text: "Continue" %>
    <% end %>
  </div>
</div>
