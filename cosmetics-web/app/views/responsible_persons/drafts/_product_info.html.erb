<div class="govuk-grid-row">
  <div class="govuk-grid-column-full">
    <div class="govuk-!-margin-top-4 govuk-!-padding-bottom-4 opss-border-bottom">
      <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
          <h1 class="govuk-heading-l govuk-!-margin-bottom-2">
            <% if @notification.product_name.present? %>
              <span class="govuk-caption-m">Product notification draft for: </span> <%= @notification.product_name %>
            <% else %>
              Add a cosmetic product
            <% end %>
          </h1>
          <p class="govuk-body opss-secondary-text">
            Complete the following tasks to add <%= @notification.product_name.present? ? "this" : "a" %> cosmetic product.
          </p>
          <%= govukDetails(summaryText: "Help with adding a product") do %>
            <p class="govuk-body">
                Your progress in completing each task is saved as a working draft until you are ready to accept and submit it. Product drafts are stored in the <a href="<%= responsible_person_draft_notifications_path(@responsible_person) %>" class="govuk-link govuk-link--no-visited-state">Draft notifications page</a> from where you can resume and complete the remaining tasks at any time.</p>
            <p class="govuk-body">
                Before you accept and submit a draft you can change information at any stage by completing individual tasks again. If you have added items (for multi-item kits) or nanomaterials, you can add or remove them individually at any stage.
            </p>
          <% end %>
        </div>
        <div class="govuk-grid-column-one-third govuk-!-margin-bottom-2">
          <% if @notification.persisted? %>
            <div class="opss-text-align-right">
              <a href="<%= delete_responsible_person_delete_notification_path(@notification.responsible_person, @notification) %>" class="govuk-link govuk-link--no-visited-state govuk-!-font-size-19 opss-text-underline-offset">
                Delete this draft
              </a>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
