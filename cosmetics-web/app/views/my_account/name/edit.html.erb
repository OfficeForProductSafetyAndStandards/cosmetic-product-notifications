<% title = "Change your name" %>
<% page_title title, errors: @user.errors.any? %>

<% content_for(:after_header) do %>
  <%= govukBackLink(text: "Back", href: my_account_path) %>
<% end %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds-from-desktop">
    <%= form_with model: @user, url: my_account_name_path, method: :patch, builder: GOVUKDesignSystemFormBuilder::FormBuilder do |form| %>
      <%= form.govuk_error_summary %>
      <h1 class="govuk-heading-l"><%= title %></h1>
      <%= form.govuk_text_field :name, label: { text: "Full name" } %>
      <%= form.govuk_submit "Continue" %>
    <% end %>
  </div>
</div>
