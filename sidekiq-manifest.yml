---
applications:
- name: mspsds-sidekiq
  memory: 256M
  buildpacks:
    - ruby_buildpack
  instances: 1
  path: .
  command: bundle exec sidekiq
  no-route: true
  health-check-type: process
  services:
    - mspsds-redis
    - mspsds-elasticsearch
    - mspsds-database