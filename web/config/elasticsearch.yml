default: &default
  :host: 'elasticsearch'
  :transport_options:
    :request:
      :timeout: 5

development:
  <<: *default

test:
  <<: *default
  :transport_options:
    :request:
      :timeout: 10000

production:
  :url: <%= ENV["VCAP_SERVICES"] && CF::App::Credentials.find_by_service_name("mspsds-elasticsearch")["uri"] %>
