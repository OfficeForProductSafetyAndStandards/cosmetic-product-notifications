- if parent.investigations.any?
  table.govuk-table
    thead.govuk-table__head
      tr.govuk-table__row
        th.govuk-table__header[scope="col"]
          | Case
        th.govuk-table__header[scope="col"]
          | Status
        th.govuk-table__header[scope="col"]
          | Assigned
        th.govuk-table__header[scope="col"]
          | Last updated
    tbody.govuk-table__body
      - parent.investigations.each do |investigation|
        tr.govuk-table__row
          td.govuk-table__cell
            = link_to investigation.title, investigation
          td.govuk-table__cell
            = investigation.status
          td.govuk-table__cell
            = investigation.assignee ? investigation.assignee.email : "Unassigned"
          td.govuk-table__cell
            = investigation.updated_at.strftime("%d %B %Y")
- else
  p
    | No cases.
