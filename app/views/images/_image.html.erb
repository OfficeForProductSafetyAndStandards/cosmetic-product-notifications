<% if image.metadata["safe"] %>
  <%= link_to render('images/image_tag', image: image, thumbnail: thumbnail), image, class: "image-with-preview" %>
<% else %>
  <%= render 'images/image_tag', image: image, thumbnail: thumbnail %>
<% end %>
