<%= form_with(model: [investigation, activity], local: true) do |form| %>
  <% if activity.errors.any? %>
    <div id="error_explanation">
      <h2 class="heading-large"><%= pluralize(activity.errors.count, "error") %> prohibited this activity from being saved:</h2>

      <ul>
      <% activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <fieldset>
      <legend><h4 class="govuk-heading-m">Activity type</h4></legend>
      <% Activity.activity_types.select { |t| !automatic? t }.each do |(type, _)| %>
        <div class="multiple-choice">
          <%= form.radio_button :activity_type, type %>
          <%= form.label :activity_type, type.titleize %>
        </div>
      <% end %>
    </fieldset>
  </div>

  <div class="form-group">
    <%= form.label :notes, :class => "form-label" %>
    <%= form.text_area :notes, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", :class => "govuk-button" %>
  </div>
<% end %>
