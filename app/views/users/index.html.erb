<% content_for :page_title, "Users - Market Surveillance" %>

<div class="users-page">
    <div class="grid-row">
        <div class="column-half">
            <h1 class="heading-xlarge">Users</h1>
            <%= link_to "Invite user", new_user_invitation_path, :class => "button" %><br /><br />
        </div>
    </div>
    <div class="grid-row">
        <% @users.each do |user| %>
            <div class="column-half">
                <%= render user %>
            </div>
        <% end %>
    </div>
</div>