<% content_for :page_title, "Case businesses - Market Surveillance & Product Safety" %>

<%= link_to 'Back to case', @investigation, class: "govuk-back-link" %>

<%= render "investigation_heading", investigation: @investigation %>

<h2 class="govuk-heading-l govuk-!-margin-bottom-2">Manage businesses</h2>
<%= link_to 'Add business', search_investigation_businesses_path(@investigation) %>

<div class="govuk-!-padding-top-8">
    <% if @investigation.businesses.any? %>
        <% @investigation.businesses.each do |business| %>
            <%= render layout: "business_summary", locals: { business: business } do %>
                <div class="form-group">
                    <%= link_to 'Remove',
                                investigation_business_path(@investigation, business),
                                method: :delete,
                                data: { confirm: 'Are you sure?' }
                    %>
                </div>
            <% end %>
        <% end %>
    <% else %>
        <p>No businesses.</p>
    <% end %>
</div>
