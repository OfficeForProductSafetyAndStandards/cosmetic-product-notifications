<% content_for :page_title, "Homepage - Market Surveillance & Product Safety" %>

<div class="home-page">
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            <h1 class="heading-xlarge">Market Surveillance &amp; Product Safety</h1>
            <%= link_to "Visit the example page", "/pages/example" %><br/>
            <%= link_to "Products", "/products" %><br/>
            <%= link_to "Investigations", "/investigations" %><br/>
            <%= link_to "Businesses", "/businesses" %><br/><br/>
            <%= render 'layouts/login_buttons' %><br/>
            <% if signed_in? && policy(User).index? %><br/>
                <p>You're an admin, this means that you can also</p>
                <%= link_to "Edit users", "/users" %>
            <% end %>
        </div>
    </div>
</div>
