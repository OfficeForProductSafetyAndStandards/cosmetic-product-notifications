- content_for :page_title, "Report an unsafe or non-compliant product"

span.govuk-caption-xl Report an unsafe or non-compliant product
h1.govuk-heading-xl
  | What product are you reporting?

= form_with model: @product, url: wizard_path, method: :put, local: true do |form|
  .govuk-grid-row
    .govuk-grid-column-full
      = render "form_components/govuk_error_summary", form: form
  .govuk-grid-row
    .govuk-grid-column-one-half
      = render "products/form", form: form, product: @product, countries: @countries, submit_text: "Continue",
              search: true
